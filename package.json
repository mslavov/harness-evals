{
  "name": "harness-evals",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "harness-evals": "dist/cli.js"
  },
  "description": "Agent-first coding-agent evaluation harness with Docker isolation, skills onboarding, and result reporting.",
  "keywords": [
    "agent-evals",
    "coding-agents",
    "skills",
    "harness-evals"
  ],
  "files": [
    "src",
    "dist",
    "skills",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "check": "tsc --noEmit",
    "build": "rm -rf dist && tsc",
    "test": "bun test"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.74.0",
    "fast-glob": "^3.3.3",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "@types/node": "^24.10.1",
    "typescript": "^6.0.2"
  }
}
